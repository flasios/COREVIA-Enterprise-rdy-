# Engine C - Distillation & Fine-Tuning Service
# Core training stack
torch>=2.2.0
transformers>=4.40.0
peft>=0.10.0
trl>=0.8.0
datasets>=2.18.0
accelerate>=0.28.0
bitsandbytes>=0.43.0

# Unsloth (optional accelerator - installed separately in Dockerfile)
# unsloth @ git+https://github.com/unslothai/unsloth.git

# GGUF conversion
sentencepiece>=0.2.0
protobuf>=4.25.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0

# HTTP client (for Ollama push + COREVIA callback)
httpx>=0.27.0

# Utilities
huggingface-hub>=0.22.0
safetensors>=0.4.0
